export class userLoginDTO {
    username: string;
    password: string;
}

export class userDTO {
    username: string;
    email: string;
    password: string;
    phoneNumber: string;
}

export class pushTokenDTO {
    pushToken: string;
    userId: string;
}